# Staging

Drop files here. Claude picks them up and routes to the right place.

## Carpetas

- `designs/` — exportaciones de Claude Design (PNG, SVG, HTML, JSON)
- `images/` — fotos/assets del cliente para el sitio
- `assets/` — logos, fuentes, iconos

Esta carpeta está en .gitignore — no se sube al repo.
